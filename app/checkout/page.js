import CheckoutForm from './checkoutfrom';
import styles from './chekoutform.module.scss';

export default function checkoutpage() {
  return (
    <main>
      <CheckoutForm classname={styles.checkoutpage} />
    </main>
  );
}
